ENGINE=django_tenants.postgresql_backend
POSTGRES_HOST="{{ tmp_back_db_host }}"
POSTGRES_PORT="{{ tmp_back_db_port }}"
POSTGRES_DB="{{ tmp_back_db_name }}"
POSTGRES_USER="{{ tmp_back_db_user }}"
POSTGRES_PASSWORD="{{ tmp_back_db_password }}"
